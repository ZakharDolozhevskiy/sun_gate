import LayerList from './components/LayerList/Layers.component.jsx';

/**
 * Render application components
 */
React.render(
  <LayerList URL={'link to external content'}/>,
  document.getElementById('wrapper')
);